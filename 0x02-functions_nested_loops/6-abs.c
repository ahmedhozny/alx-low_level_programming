#include "main.h"

/**
 * _isabs - Returns the absolute value of a given integer.
 * @c: integer to be checked
 * Return: The absolute integer.
 */
int _isabs(int c)
{
	return ((c < 0) ? c * -1 : c);
}

